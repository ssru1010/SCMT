# ====================================================
# pytest.ini — base configuration for SOMT test suite
# ====================================================
[pytest]
minversion = 7.0
addopts = -v --disable-warnings
testpaths = tests
python_files = test_*.py
filterwarnings =
    ignore::UserWarning
